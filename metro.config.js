
/*
const { getDefaultConfig, assetExts } = require('metro-config');
module.exports = (async () => {
    const defaultConfig = await getDefaultConfig();
    return {
        resolver: {
            // Add bin and png to assetExts
            assetExts: [...assetExts, 'bin', 'png'],
        }
    };
})();*/